// Contains method to check if score is valid
interface IScore {
	// Checks if the class contains a valid score
	boolean isValid();
	
	// Returns the name of the winner. Return true if player 1 won else false
	boolean getWinner();
}
